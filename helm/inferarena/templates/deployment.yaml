{{- if not .Values.job.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "inferarena.fullname" . }}
  labels:
    {{- include "inferarena.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "inferarena.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "inferarena.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "inferarena.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: inferarena
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{ toYaml .Values.experiment.command | nindent 12 }}
          args:
            {{- range .Values.experiment.args }}
            - {{ . | quote }}
            {{- end }}
            {{- range .Values.extraArgs }}
            - {{ . | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
            {{- if .Values.persistence.enabled }}
            - name: outputs
              mountPath: /app/inferarena_outputs
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "inferarena.configMapName" . }}
        {{- if .Values.persistence.enabled }}
        - name: outputs
          persistentVolumeClaim:
            claimName: {{ include "inferarena.fullname" . }}-outputs
        {{- end }}
{{- end }}
